#!/usr/bin/make -f

VERSION=1.71.0

%:
	dh $@

override_dh_auto_clean:
	ischroot || bash fetch.sh upstream $(VERSION) \
		43f0b7551dcb363de7360a9d8bda777fced722c60acdce9e4a6d62b50ae83997 \
		9b0dbf715d75cd91bc4b5c0c57dc9c40ee8076530278fc92bdfb8f71131d798f
#	ischroot || just version=$(VERSION) fetch

override_dh_auto_build:
	just version=$(VERSION)

override_dh_auto_install:
	just version=$(VERSION) rootdir=debian/tmp install

override_dh_strip:

override_dh_shlibdeps:
	dh_shlibdeps -Xclippy